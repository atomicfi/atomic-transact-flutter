<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="AppiumTestEnvironment"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- The suite starts this activity by explicit component with the launch configuration as
             TRANSACT_* extras. singleTask matches the native test app. The theme's action bar shows
             the label as a native TextView, which is what the suite's
             //*[@text="AppiumTestEnvironment"] finds: Flutter's own text only reaches content-desc. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/HarnessTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- The suite sends pause/resume with
             `am broadcast -a <applicationId>.COMMAND -n <applicationId>/.TransactCommandReceiver`. -->
        <receiver
            android:name=".TransactCommandReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="${applicationId}.COMMAND"/>
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
